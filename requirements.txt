# 企业知识库问答系统 - 实际运行依赖（与当前环境一致，已验证）

# Web框架
fastapi==0.139.0
uvicorn==0.50.2
python-multipart==0.0.32

# 配置
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.2.2

# 向量数据库（内置 ONNX MiniLM embedding）
chromadb==1.5.9
onnxruntime==1.30.0

# 文档处理
langchain-text-splitters==1.1.2
PyPDF2==3.0.1
python-docx==1.2.0

# LLM（可选：在 .env 配置 OPENAI_API_KEY 后启用生成式回答）
openai==2.44.0
